h1 Corpuses

form action="/corpuses" method="post"
  input.u-full-width type="submit" value="New Corpus"

table.u-full-width
  tbody
    tr
      th id
      th documents
      th
      th
    - @corpuses.each_with_index do |corpus, index|
      - next unless corpus
      tr
        td = index
        td = corpus.documents.size
        td : a.button href="/corpuses/#{index}" Show
        td
          form action="/corpuses/#{index}" method="post"
            input type="hidden" name="_method" value="delete"
            input type="submit" value="Delete"
